# 工作学习平台 · 使用说明

> 一个单文件网页应用：可以在网上用网址打开，也可以下载到本地双击打开。数据保存在你自己的电脑上，不需要注册、不需要安装任何东西。

## 一、怎么打开

### 方式一：在线打开（在单位最方便，什么都不用装）

在浏览器地址栏输入下面这个网址，建议用 Chrome 或 Microsoft Edge：

```
https://<你的用户名>.github.io/<仓库名>/
```

打开后把这个网址加到**书签栏**（Ctrl + D），以后点一下就能用。

### 方式二：本地打开（不联网也能用）

1. 把 `index.html` 下载到电脑上，双击就会用默认浏览器打开（建议用 Chrome 或 Microsoft Edge）。
2. 想放到桌面或任务栏：右键 `index.html` → 发送到 → 桌面快捷方式。
3. 建议固定成浏览器的一个标签页，全天开着，随时记一笔。

## 二、核心功能

### 1. 记录任务与进度（「任务」页）

- 点右上角「+ 新建任务」：写清任务内容，选类型（工作 / 学习 / 生活）、优先级、截止日期、计划执行日、预计工时。
- **子步骤**：把一件事拆成几个小步骤，勾选小步骤会自动换算成任务进度。
- **进度**：用列表里的「+」按钮快速加 10%，或在任务详情里拖动进度条精确设置，达到 100% 自动标记为已完成。
- **计时**：任务行上的 ▶ 按钮开始计时，做完了点「结束」，系统自动把这段时间记进「投入时长」。
- 每次改动都会自动生成一条进展记录，当天就成了日志的原始素材。
- 「看板」视图可以按住卡片拖到不同状态列，直观看到手上事情的整体分布。

### 2. 长期学习区（「学习」页）

不是一次性任务，而是「一门课、一本书、一个专题」这种要跟几周几个月的事，放在这里。

- **新建学习项目**：填名称、类型（书籍 / 课程 / 专题 / 技能 / 认证）、学习目标、资料来源，可关联到某个目标。
- **学习单元**：把课程拆成章节 / 课时；在编辑弹窗里可以一个个加，也可以点「批量粘贴」把整份目录一次贴进来。勾选单元，进度环自动更新，全部勾完自动标记为已完成。
- **学习笔记**：卡片上直接写一条、回车即存；笔记会汇总进日报的「学习进展」。
- **学习计时**：点 ▶ 开始学习，结束自动记入该项目的学习时长（和任务计时是同一套，互不冲突）。
- **下一步**：每个项目都有一个「下一步」提示；点「加入今日」会把它变成一条今天的任务，进入今日聚焦。
- 顶部四个数字：学习中项目、本周学习、累计学习、连续学习天数；下方有近 30 天学习节奏图和最近学习记录。

### 3. 自动生成日报 / 周报（「报告」页）

- 选「日报 / 周报 / 月报」，选好时间，内容会根据你的任务记录、进度更新、计时、随记和**学习项目进展**自动生成。
- 生成的是可编辑草稿：直接在上面改，改完点「复制」「下载 .md」，或点「存入日志库」长期保存。
- 左侧可以勾掉不想出现的章节（比如今天没有风险，就去掉「问题与风险」；学习进展也可以单独关掉），章节编号会自动顺延。
- 「预览」按钮可以看排版后的效果，方便直接贴到钉钉 / 飞书 / 邮件里。
- 打开应用时，如果有昨天的记录，会自动把昨天的日报存进日志库（可在「数据」页关闭）。

### 4. 规划工作与学习（「计划」「目标」页）

- **计划页**：一周七天的排期表。左边是「未排期」的任务池，按住任务卡片拖到某一天就排好了；拖回左边取消排期。下面还有每天的完成数与投入时长对比。
- **目标页**：新建长期目标（如「通过 CET-6」），拆成几个里程碑，勾选里程碑自动算目标进度；任务和学习项目都可以关联到某个目标。

### 5. 今日工作台（「今日」页）

- 顶部四个数字：今日完成、待推进、今日投入、连续记录天数。
- 「今日聚焦」：今天要推进的事；「待办池」：还没排期的任务，一键安排到今天。
- 「今日学习」：正在学的项目、各自的下一步，可以就地开始计时或加入今日任务。
- 「快速记录」：一句话写下今天发生的事（会议结论、踩过的坑），会自动进入日报的「今日随记」。

## 三、数据与备份（重要）

- 所有数据保存在**本机浏览器**里，不会上传到任何服务器。
- 在单位电脑和家里电脑打开的是**两份独立的数据**，不会自动同步。下班前在「数据」页点一次**「导出 JSON 备份」**，回家导入即可（在家改完，第二天上班再导入回来）。
- 换电脑、重装系统、清理浏览器数据前，请到「数据」页点**「导出 JSON 备份」**；在新电脑上用**「导入 JSON 备份」**恢复。
- 「导出全部日志（Markdown）」会把任务清单、学习项目（含单元与笔记）、目标和所有已保存日志导成一个文件，方便归档或交给别人。
- 想先体验效果，在「数据」页点「载入示例数据」；「清空全部数据」会删除所有内容，请先备份。

## 四、快捷键

| 按键 | 作用 |
| --- | --- |
| `N` | 新建任务 |
| `/` | 跳到搜索框，搜索任务 |
| `Enter` | 在「添加单元」「记一条学习笔记」输入框里快速提交 |
| `Esc` | 关闭弹窗 |
| `Ctrl` + `Enter` | 在任务 / 目标 / 学习项目弹窗里直接保存 |

## 五、几点使用建议

- 每天开始工作前，先在「计划」页把今天的 3～5 件事排到当天；结束时到「今日」页写一句随记。
- 计时不用很精确，但要养成"开始做就点开始"的习惯，投入时长的数据才可信。
- 长期学习项目不用贪多，同时进行 2～3 个就够，剩下的放「未开始」。
- 每周五花十分钟看看「学习」页的进度环和近 30 天节奏图，比每周重新回忆有效得多。
- 周五下班前点开「报告 → 周报」，改两句话就能发出去，不用再回忆一周干了什么。